[pytest]
# Bare `pytest` only collects unit tests (moto). Run UI tests with: pytest tests_ui
testpaths = tests

# So tests_ui can `import sample_polls` from the tests/ folder (shared fixtures).
pythonpath = tests
